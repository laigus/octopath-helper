---
description: 项目编码规范和注意事项
globs: "**/*.py"
alwaysApply: false
---

# 编码规范

## Python 环境
- 虚拟环境：`d:\AI\octopath-helper\.venv`
- pip 安装使用清华源：`-i https://pypi.tuna.tsinghua.edu.cn/simple`
- 需要设置 `$env:NO_PROXY="*"` 绕过系统代理

## PyQt6 注意事项
- 无边框窗口 + 透明背景 + 置顶：`FramelessWindowHint | WindowStaysOnTopHint | WA_TranslucentBackground`
- 不使用 QTabWidget（会产生多余边框/横线），改用 QPushButton + QStackedWidget 模拟标签页
- SVG 图标通过 `PyQt6.QtSvg.QSvgRenderer` 渲染到 QPixmap
- 字体通过 `QFontDatabase.addApplicationFont()` 动态加载

## 样式系统
- QSS 样式使用自定义 `_s()` 函数做 `$variable` 替换（避免 CSS `{}` 与 Python f-string 冲突）
- 替换按变量名长度倒序执行（防止 `$fg` 匹配到 `$fg2`）
- 黑/白两套主题色值在 `_apply_theme()` 中定义

## Windows DWM API
- `ui/acrylic.py` 通过 ctypes 调用 `user32.dll` 和 `dwmapi.dll`
- Acrylic 磨砂 → 圆角 → DWM 阴影三步启用
- tint_color 格式：AABBGGRR（alpha 极低实现高透明）

## 数据持久化
- 所有状态保存到 `data/state.json`（勾选、窗口位置、主题）
- 每次勾选变化自动保存

## 文件组织
- 所有 UI 逻辑集中在 `main.py`（项目较小，不需要拆分）
- Windows API 封装放 `ui/acrylic.py`
- 字体文件放 `assets/fonts/`
- 运行时数据放 `data/`
